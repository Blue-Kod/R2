# AGENTS.md — Контекст проекта R2

## Objective

Довести до готового состояния интеграцию двухзонной FK/IK-модели в `robov_core/arm_kinematics.py`.
Кран-зона (`|theta0|>90`, ch4>135) — старая Rz-модель; нижняя зона (`|theta0|<=90`, ch4<=135) — новая Ry-модель с аффинным калибровочным маппингом.
Директива пользователя: «финишная прямая, делай до готового проекта».

---

## Important Details

### Физическая модель (валидирована tinyik, err ≤0.6 мм на 8 якорях)

Цепочка: `[±115,0,0],'y','x',[0,−330,0],'x',[0,−220,0]`, углы `(t4,−t2,−t3)`.
EE = `(115,0,0) + 330·upper + 220·lower`, где:
- `upper = [s4·s2, −c2, c4·s2]`
- `lower = [s4·s23, −c23, c4·s23]`

### Двухзонная диспетчеризация в `fk()`

- `_pan_mirror(t1) = |t1| > 90` → **кран-зона**: старая Rz-модель (err 0.4–55 мм).
- Иначе → **нижняя зона**: Ry-модель с аффинным маппингом (err 0.6–14 мм).
- `theta_from_commands` / `to_servo_commands` **не менялись**.

### NATURAL_AFFINE (в коде, значение = a+b·t0+c·t1; для t3p: a+b·t0+c·t2)

| Жёсткая рука | t4             | t2p             | t3p          |
|-------------|----------------|-----------------|--------------|
| **Правая**  | (−58.09, −0.957, 0.647) | (−10.745, −0.038, 1.387) | (20.48, −0.108, 0.476) |
| **Левая**   | (58.09, −0.957, −0.647) | (−10.745, 0.038, 1.387)  | (20.48, 0.108, 0.476)  |

ВАЖНО: формула левой руки **исправлена** относительно прежнего плана — левая есть **зеркало** правой (не same-as-right).

### Зеркало левой руки

`theta_L = (−t0, t1, t2)` даёт точное X-зеркало правой в обеих зонах.
x-компонента левой = `+s4·s2` (не σ·s4·s2), база = −115.
Проверено численно на 3 якорях: mirr=True для (169,66,137), (184,92,62), (184,118,62).

### Калибровка нижней зоны по командам (правая)

```
t4 = 0.957·ch4 + 0.647·ch1 − 188.5
t2 = 1.387·ch1 − 199.7 + 0.038·ch4
t3 = −0.476·ch6 + 101.3 + 0.108·ch4
```

Подогнана **только по 3 якорям** с ch1≈169–184. Экстраполяция на ch1≈47 (задние цели) **неверна**.

### Базовая линия

Git HEAD `5be286b` «Зеркало для режима крана» — одно-зонная Rz + зеркальный кран-путь.
Стол решался кран-позой ch4 195–250.

### Константы IK

```python
IK_ERR_OK = 3.0
IK_TOLERANCE_MM = 10.0
W_NAT = 0.3
GRID_STEPS = ((8.0, None), (2.0, 10.0), (0.4, 2.0), (0.08, 0.5))
IK_CLEARANCE_MARGIN = 0.0
```

Диапазон shoulder_z для правой: `(-225, 35)` — покрывает обе зоны.

### DEFAULT_POSE и прочее

- Правая: ch1=135, ch4=45, ch6=180
- Левая: ch2=135, ch5=45, ch7=180
- `INVERTED_CHANNELS = {2, 4, 8}`
- `DEFAULT_OFFSETS`: ch4=−10

### Окружение

- Python 3.14.5
- venv: `/home/bluekod/Проекты/R2/.venv`
- numpy 1.26.4, cv2 4.9.0
- scipy **намеренно отсутствует** (нет колёс для Py3.14/numpy1.26; numpy≥2 ломает cv2)

### Деплой

- `orangepi@192.168.0.165` (пароль `orangepi`)
- Сервис: `r2-robot`
- Директория: `/home/orangepi/R2/`
- Launcher делает `git pull` при старте

---

## Work State

### Completed

- [x] `fk()`, `_fk_grid_positions`, `_fk_grid_elbows`, `_natural_penalty_series` — двухзонные (np.where по маске |t1|>90).
- [x] Хелперы `_natural_angles`, `_fk_natural_pose`.
- [x] Зеркальный путь удалён из `ik_solve` (всегда `_solve_natural(wanted, left, start)`).
- [x] `_reflect_theta` / `_reflect_pose` удалены.
- [x] Self-test обновлён: зеркало `fk((-right[0],right[1],right[2]),left=True)`.
- [x] Добавлены `natural_anchors` ((169,66,137)→(0,−300,400) tol 5.0; (184,92,62)→(250,0,400) tol 5.0; (184,118,62)→(400,0,300) tol 20.0).
- [x] Проверено: grid-функции совпадают со скалярными `fk()` / `_natural_penalty_series`.
- [x] Цели стола решаются напрямую в природной зоне:
  - (0,−300,400) → ch4=66 err 3.3–3.9 мм
  - (0,−260,400) err 0.2
  - (0,−250,400) err 1.1
  - (0,−280,400) err 1.3
- [x] Левая зеркалит правую ((21.4,33.3,47.5) vs (−21.4,33.3,47.5), err 3.9 обе, ee R=(0.9,−298,396.7), L=(−0.6,−298,396.7)).
- [x] Правые нижние цели (250,0,400) err 0.2, (400,0,300) err 0.3 — прежняя проблема (err 25.8, упор в ch4=135) решена.
- [x] Сравнение с baseline:
  - (0,−200,−300) → theta=(−29.9,−87.8,95.6) err 0.2
  - (0,−300,200) → theta=(−201,112.7,95.6) err 0.1
  - (0,−300,400) → theta=(−201,111.7,43.2) err 0.1
- [x] Перечитан `dev/tinyik_arm.py` (прототип двухзонного маппинга, строки 8–14).

### Active (regressions)

1. **Self-test падает на строке 758**: `assert 195.0 <= ch <= 250.0` — табличный тест требует кран ch4 195–250, а теперь приходит природное ch4=66 (err 3.3, ok=True). Блок табличных проверок ещё не отредактирован.

2. **РЕГРЕССИЯ 1 — задняя цель (0,−200,−300)**: ik_solve err 153.3 (baseline 0.2).
   - Причина: `|theta0|=29.9<=90` → нижняя зона → аффинная Ry-калибровка экстраполирована на ch1≈47 (вне диапазона подгонки 169–184) → неверный FK.
   - Baseline fk((-29.9,-87.8,95.6)) = (0,−199.9,−299.9), err=0.18.
   - Наш fk() для этой же позы: (556, 115, −29) —完全不同.

3. **РЕГРЕССИЯ 2 — (0,−300,200)**: ik_solve err 12.8 (baseline 0.1 через кран).
   - Причины:
     - Refinement сходится к природному локальному оптимуму (тонкая сетка шаг 2 находит err 3.6 при theta=(−24,9,152)).
     - Natural penalty W_NAT=0.3 штрафует кран-позы (пример: 37.12 для (−210,−95,−90)).
     - Зеркальный путь (источник кран-решений) удалён.
   - Кран-решение существует: fk((-201,113,94)) в двухзонной модели даёт err ~5.4 (базовая Rz-формула не менялась).

4. **Левая рука**:
   - (150,−300,400) левая err 16.1 (правая 0.7).
   - (400,0,300) левая через прямой солвер err 59.2.

5. **Браузерная FK `ikFk`** в `templates/index.html` (~строки 318, 330–346) ещё не синхронизирована с двухзонной моделью.

### Blocked

- Калибровка нижней зоны достоверна только вблизи ch1≈169–184 (передняя панорама).
- Для задних/боковых целей нужен либо fallback на старую Rz вне «конверта» калибровки, либо новые замеры на роботе, либо документированное снижение точности.
- Доступа к роботу для повторных замеров пока нет; деплой отложен до корректности кода.

---

## Root Cause Analysis

### Почему аффинный маппинг ломается для задних целей

Аффинный маппинг калиброван по 3 якорям:
- (169,66,137) → (0,−300,400)
- (184,92,62) → (250,0,400)
- (184,118,62) → (400,0,300)

Все якоря с ch1 ∈ {169, 184} — передняя панорама (θ1 = ch1−135 ∈ [34, 49]).
Для задней цели (0,−200,−300) нужна панорама ch1=47 → θ1=−88 → за пределами калибровки.
Аффинная экстраполяция даёт физические углы (t4=−86.3, t2=−131.4, t3=69.2),
а tinyik IK находит (t4=21, t2=−22.7, t3=−95.6) —完全不同.

### Почему зеркальный кран-путь больше не работает

В baseline зеркальный путь отражал natural-решение в кран-позу и использовал Rz-FK.
В двухзонной модели зеркальный кран-решение после отражения попадает в нижнюю зону (|θ0|≤90)
и вычисляется через аффинный Ry-FK, который неверен для кран-poses → err 148.

### Возможные решения (обсудить с пользователем)

1. **Fallback на Rz для нижней зоны вне калибровочного конверта**:
   - Определить конверт: ch1 ∈ [150, 200] (или θ1 ∈ [15, 65]).
   - Вне конверта: использовать старую Rz-модель (как baseline).
   - Плюс: все существующие тесты проходят, базовая линия сохраняется.
   - Минус: разрыв на границе конверта; нижняя зона не повсеместна.

2. **Убрать/уменьшить natural penalty для кран-зоны**:
   - Позволить solver находить кран-решения без штрафа.
   - Плюс: (0,−300,200) снова решится краном.
   - Минус:失去 эргономичность (elbow up preference).

3. **Расширить калибровку** (потребовать замеры на роботе):
   - Добавить 3–5 якорей для боковых/задних панорам.
   - Плюс: двухзонная модель станет глобально точной.
   - Минус: нужен робот для замеров.

4. **Гибрид**: прямой двухзонный solve + fallback на Rz когда accuracy < threshold.
   - Плюс: best of both worlds.
   - Минус: сложность; нужно решить, когда fallback.

---

## File Map

```
robov_core/
  arm_kinematics.py   ← двухзонная интеграция, NATURAL_AFFINE, падающий self-test (строка 758)
  high_level.py       ← вызовы theta_from_commands + ik_solve; изменён в рабочем дереве (+10) — проверить
  servo.py            ← DEFAULT_POSE, INVERTED_CHANNELS={2,4,8}, DEFAULT_OFFSETS (ch4=−10)
  web.py              ← /api/ik, /api/ik/move, /api/servo/<ch>/<angle>
  tinyik/             ← неотслеживаемая чистая numpy-копия tinyik (LM-solver)

templates/
  index.html          ← браузерная ikFk (строки 318, 330–346) — требует двухзонной синхронизации

dev/
  tinyik_arm.py       ← прототип двухзонного маппинга (строки 8–14 комментарий) — эталон намерения
```

---

## Commands

```bash
# Запуск self-test
python -m robov_core.arm_kinematics

# Линтер/типчекер (если есть)
# В текущем репо линтер/типчекер не настроен

# Запуск сервера
python main.py

# Деплой на робота
git push
# На роботе: launcher делает git pull автоматически
# Или вручную:
ssh orangepi@192.168.0.165 'cd /home/orangepi/R2 && git pull && sudo systemctl restart r2-robot'
```

---

## Git State

- Ветка: `main`
- HEAD: `5be286b` «Зеркало для режима крана»
- Не закоммичены: `robov_core/arm_kinematics.py` (+213/−), `robov_core/high_level.py` (+10/−)
- Неотслеживаемая: `robov_core/tinyik/`
